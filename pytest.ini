[pytest]
testpaths = tests/
markers =
    live: test performs a real network call to a broker/live API (excluded from CI; run with -m live)
addopts =
    -r fEsxXw
    -vvv
    --doctest-modules
    --ignore setup.py